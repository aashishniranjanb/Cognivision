# Camera Configuration
# Use 0 for webcam, "mock" for synthetic test feed, or RTSP URL: rtsp://user:pass@host:554/stream
ENTRY_CAMERA_URL=mock
EXIT_CAMERA_URL=mock

# Buffer Configuration
FRAME_BUFFER_MAXSIZE=30
DROP_POLICY=drop_oldest

# Logging & Metrics
LOG_LEVEL=INFO
METRICS_WINDOW_SEC=3.0
